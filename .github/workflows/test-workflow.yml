name: Test Workflow

on:
  push:
    branches:
      - fix/metadata-seo-and-design-tokens

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Test workflow running"